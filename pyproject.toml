[project]
name = "grok-cookie-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "fastapi>=0.139.0",
    "uvicorn>=0.49.0",
    "websockets>=16.0",
]

[project.scripts]
grok-cookie-mcp = "server:main"
